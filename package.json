{
  "name": "@kilip/api-client",
  "version": "0.0.0",
  "private": true,
  "repository": "kilip/api-client",
  "description": "ApiPlatform reusable client library",
  "type": "module",
  "scripts": {
    "build": "FORCE_COLOR=1 pnpm --filter './packages/**' prepack",
    "build:stub": "pnpm --filter './packages/**' prepack --stub",
    "cleanup": "rimraf 'packages/**/node_modules'",
    "lint": "eslint --ext .vue,.ts,.js,.mjs .",
    "test": "vitest",
    "test:unit": "JITI_ESM_RESOLVE=1 vitest run --dir packages",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@kilip/api-core": "workspace:*",
    "@types/node": "^18.11.18",
    "@types/rimraf": "^3.0.2",
    "@typescript-eslint/parser": "^5.49.0",
    "eslint": "^8.33.0",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.4",
    "unbuild": "^1.1.1",
    "vitest": "^0.28.3",
    "vitest-fetch-mock": "^0.2.1"
  },
  "exclude": [
    "**/dist/**",
    "**/test/fixtures/**"
  ]
}
